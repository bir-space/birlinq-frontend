node_modules/
.next/
out/
build/
.env*.local
.env
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Expo
.expo/
.expo-export-check/
expo-env.d.ts
# Native projects are generated by `expo prebuild` (CNG) — app.json is the source
# of truth, so a committed copy would silently diverge from it.
apps/mobile/android/
apps/mobile/ios/
